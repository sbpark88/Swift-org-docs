import Foundation

// 1. Compound Assignment (할당 연산자)
// 상수, 변수의 값을 초기화 시키거나 변경한다.
let a = 10
var b = 7
b = a
print("b is \(b)")

// 튜플을 이용해 한 번에 여러 값을 할당할 수 있다.
let (x, y, z) = (10, 5, 12)
print("x is \(x), y is \(y), z is \(z)")
